use num_bigint::{BigInt, RandBigInt};
// use rand::thread_rng;
// use num_traits::{One, Zero};
// use sha2::{Sha256, Digest};


/*fn mod(base:&BigInt, exp:&BigInt, modulus:&BigInt) -> BigInt {
    base.modpow(exp, modulus) // base^exp % modulus
} */ 
fn main() {
    println!("{}", BigInt::from(2));   
}
